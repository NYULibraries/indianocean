---
export interface Props{
    href: string;
    src: string;
    alt: string;
}
const {href, src, alt} = Astro.props;
---
<div class="logo">
    <a href={href} target="_blank">
      <img src={src} alt={alt}>
    </a>
</div>