---
import BookLayout from '../../layouts/BookLayout.astro'
import Book from '../../components/Content/Books/Book';

export function getDocumentTypeByBundle(bundle: string): string | undefined {
  return {
    'dlts_book': 'books',
    'dlts_map': 'maps',
  }[bundle]
}

// const url = window.location.href;
// const splitUrl = url.split('/')
// const identifier  = splitUrl[splitUrl.length-1];
// const pageTitle = `Page ${identifier}`
// const pageDescription = `This is page ${identifier}`
const pageTitle = 'a'
const pageDescription = 'b'

---

<BookLayout
  title={pageTitle}
  description={pageDescription}
>
	<Book client:only/>
</BookLayout>

<style lang="scss"></style>


